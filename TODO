* SLE_11 support in server part
* port the scripts that nodejs-packaging has here.
* support nsp security check for the bundle.
* consider to support the grunt auto tasks
* rspec unit tests
* handle recursive bower.json !important
* bundleDependencies in package.json
* optionalDependencies
* devDependencies
* noarch package !important
* bower in provides
* [server]check json consistency for all the modules instead of just the main module
* i386 buildroot are treated as i586
